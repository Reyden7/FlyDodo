import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.loomstudio.flydodo',
  appName: 'FlyDodo!',
  webDir: 'dist',
  backgroundColor: '#73d8ff',
  android: {
    backgroundColor: '#73d8ff',
  },
};

export default config;
